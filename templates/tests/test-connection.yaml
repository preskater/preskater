apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "preskater.fullname" . }}-test-connection"
  labels:
    {{- include "preskater.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "preskater.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
